# akshay-github-meta

The **personal GitHub profile** repo — pushed to `palimkarakshay/palimkarakshay`
(the magic profile-README repo; local dir named `akshay-github-meta` so the
auto-sync cron and shell globs see it). Root `README.md` renders on
https://github.com/palimkarakshay.

- Lumivara product line: **Sites** (personal-brand surface, not a product).
- Package manager: none (markdown only, no build).
- Public by GitHub requirement (profile READMEs must be public).

## Commands
- Develop: edit `README.md`; preview with any markdown renderer.
- Verify: `git push` then check https://github.com/palimkarakshay renders it.

## Layout
- `README.md` — SAP×AI specialist positioning: ABAP AI suite table (abap-mcp
  with live npm shields.io badge, vscode/adt/mcp-kit), other OSS kits, RAP Dojo
  + Lumivara pointer, building-with strip, contact links.

## Gotchas / invariants
- Keep positioning in lockstep with https://lumivara.tech and the org profile
  (`lumivara-github-meta` → `lumivarahq/.github`): reputation-led SAP×AI
  specialist; never interview-bait framing.
- The npm badge hotlinks shields.io (`/npm/v/abap-mcp`) — auto-updates on
  publish, breaks offline renderers (fine).
- OSS stays on this personal handle; product/commercial repos under lumivarahq
  (standing rule since 2026-06-09).
